# PostgreSQL Client Authentication Configuration File
# ===================================================
#
# This file controls authentication and authorization for PostgreSQL connections.
# Format: TYPE  DATABASE  USER  ADDRESS  METHOD  [OPTIONS]

# "local" is for Unix domain socket connections only
local   all             all                                     trust

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256
host    all             all             0.0.0.0/0               scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256

# Allow replication connections from any host (for CDC)
# In production, you should restrict this to specific IP addresses
host    replication     all             0.0.0.0/0               scram-sha-256

# Allow logical replication connections
host    all             all             0.0.0.0/0               scram-sha-256
